# Demo vault

Fake notes for product screenshots. Not your real writing.

App reads from `~/Documents/KensEditor/`. To shoot Command-P without exposing real pages:

```bash
# from repo root — quit the app first
./scripts/use-demo-vault.sh on
# open app, ⌘P, screenshot

./scripts/use-demo-vault.sh off
# real vault restored
```

Pinned for a nicer grid top row:
- Ship the thing.
- What this app is not
- Idea for the about page
